COMPILADOR = gcc

ARCHIVOS_A_COMPILAR = impresor.c suma.c

VARIABLES = -o

NOMBRE_EJECUTABLE = ejecutar

all: 
	$(COMPILADOR) $(ARCHIVOS_A_COMPILAR) $(VARIABLES) $(NOMBRE_EJECUTABLE)

clean:
	 rm $(NOMBRE_EJECUTABLE)